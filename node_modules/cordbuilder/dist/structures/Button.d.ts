import { ButtonBuilder } from "discord.js";
export declare class Button {
    static from(source: string): ButtonBuilder;
    static edit(btn: ButtonBuilder, source: string): void;
    private static validateProperties;
    private static validateStyle;
    private static validateLabel;
    private static validateLink;
    private static validateCustomId;
    private static validateEmoji;
    private static validateDisabled;
    private static validateValue;
}
